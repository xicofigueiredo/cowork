<p>TOConline failed to issue an invoice for a paid order.</p>

<p>
  <strong>Order:</strong> #<%= @order.id %><br>
  <strong>Customer:</strong> <%= @order.customer_name %> &lt;<%= @user.email %>&gt;<br>
  <strong>Plan:</strong> <%= @order.plan_label %> (<%= @order.total_with_iva_euros %>)<br>
  <strong>Internal ref:</strong> <%= @order.invoice_number %>
</p>

<p>
  <strong>Error:</strong><br>
  <code><%= @error_message %></code>
</p>

<p>Fix on the server (in <code>~/cowork</code>):</p>
<pre style="white-space: pre-wrap; font-family: ui-monospace, monospace; font-size: 13px;"># Preferred: probe + auto-reauth + backfill missing invoices, emails the log
docker compose exec -u rails server ./bin/rails toconline:health

# Status only
docker compose exec -u rails server ./bin/rails toconline:status

# If unauthorized_client and auto-reauth failed:
docker compose exec -u rails server ./bin/rails toconline:authorize_url
docker compose exec -u rails -e CODE='...' server ./bin/rails toconline:auth

# Invoice all paid orders still missing a fatura
docker compose exec -u rails server ./bin/rails toconline:backfill</pre>
